# HireMe.AI 前端

这是 HireMe.AI 的 React + TypeScript + Vite 前端应用。

## 本地开发

```bash
npm ci
npm run dev
```

开发服务器默认访问后端 `http://localhost:8000`，并通过 `vite.config.ts` 将 `/api` 请求代理到后端。

## 检查命令

```bash
npm run lint
npm run build
```

完整启动方式、隐私说明和后端配置请查看仓库根目录的 [README.md](../README.md)。
